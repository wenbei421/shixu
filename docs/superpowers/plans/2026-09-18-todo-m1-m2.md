# 待办 M1+M2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将项目/标签/设置提升为 `sys_*` 共用表，并落地待办数据层 + 三栏骨架（对齐 Muse）。

**Architecture:** SQLite migration 改名并扩展字段；Muse 改指向 `sys_*`；新增 `todo_*` 表与 Rust commands；前端按 Muse 分层（lib/store/page）。

**Tech Stack:** Tauri 2、sqlx、Vue 3、Pinia、TypeScript

---

### Task 1: Migration `sys_*` + `todo_*`

**Files:**
- Create: `src-tauri/migrations/0005_sys_shared_and_todo.sql`
- Modify: Muse 相关触发器/视图（在同文件 DROP/CREATE）

- [ ] 改名 `muse_projects/tags/settings` → `sys_*`
- [ ] ADD COLUMN 扩展字段
- [ ] 重建引用 `sys_*` 的 Muse 触发器与视图
- [ ] 创建 `todo_tasks`、`todo_task_tags` 及索引

### Task 2: 更新 Muse Rust SQL

**Files:**
- Modify: `src-tauri/src/commands/muse.rs`

- [ ] 所有 `muse_projects`/`muse_tags`/`muse_settings` SQL → `sys_*`
- [ ] 冒烟：list projects/tags 仍可用

### Task 3: Todo commands

**Files:**
- Create: `src-tauri/src/commands/todo.rs`
- Modify: `src-tauri/src/commands/mod.rs`、`src-tauri/src/lib.rs`

- [ ] list/create/update/delete/complete 任务
- [ ] 按 perspective 筛选；绑定 project/tags
- [ ] 注册 invoke handlers

### Task 4: 前端骨架 + 数据接线

**Files:**
- Create: `src/lib/todo.ts`、`src/stores/todo.ts`、`src/pages/todo.vue`、基础组件
- Modify: router、sidebar、i18n

- [ ] 路由与导航
- [ ] 三栏布局 + 列表 CRUD UI
- [ ] 调用 commands 刷新 store

### Task 5: 验证

- [ ] `cargo check`（或针对性测试）通过
- [ ] Muse 列表仍正常；Todo 可增删完成
